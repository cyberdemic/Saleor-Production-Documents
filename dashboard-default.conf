# Nginx default.conf
server {
    # Default saleor dashboard port is 9000
    listen       9005;
    server_name  example.com;
    root   /app/dashboard/;

    location /dashboard/ {
        alias /app/dashboard/;
        index  index.html;
        try_files $uri $uri/ /dashboard/index.html;
    }
}

